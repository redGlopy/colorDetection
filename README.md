this is a project that i made to learn how to use c++ andopenC.

by defaulte it is suppoused to track bright green gloves but i can change it manually with the sliders

this program is made for windows , i have no idea if this program works on mac or linux.

u will probobly see a lot of files named colorDetection and it is because this program used to detect only colors but when i started with detecting hand i was to lazy to change it so dont worry.

dependencies:
  CMake(version: 4.1.0).
  a cpp compiler(i use g++).
  openCV(version: 4.12.0).
  win32(the API that lets u move the cursor and more).

to install the program head to the newest release and install the .exe file

the instructions file is inside of the folder for the program and please read it .

WARNING: the program is not prefect so please open a new desktop when ur using it to not accidentally click on random things.
